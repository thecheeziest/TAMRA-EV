import styled from 'styled-components'

// TamraeveWrap
export const TamraeveWrap = styled.div`
/* width: 100%; min-width:1280px; max-width:1920px;
margin:0 auto;
overflow:hidden; position:relative;
user-select: none;
header {
    z-index:100; position: absolute;
    width:100%;
    left: 0; top: 0;
    .inner {
        position: relative; left: 50%; top:0;
        transform: translateX(-50%);
        width: 1600px; color:#fff;
        padding:34px 0; margin-left:-600px;
        display: flex;
        h1 { font-size: 0; }
    }
}
#container {
    #visual {
        position: relative;
    }
}
footer {
    width:100%; text-align:center; padding:60px 0; background:#303030;
    .inner {
        h3 { }
        em { display:block; color:#707070; font-family:'ht_r'; font-size:14px; margin-top:40px; }
    }
} */
`

// TamraeveNavBox
export const TamraeveNavBox = styled.div`
/* .bg {position: fixed; right: 0px; top: 0; width: 100vw; height: 100vh; background: black; opacity: 0.7; display: none;
&.on {display: block;} }
.all-menu {

}
nav {
    position: fixed; right: 0px; top: -70vh;
    width: 100vw; height: 70vh;
    background: #FFF;
    transition: 0.3s;
    display: flex;
    justify-content: space-between;
    &.on { top: 0; }
    .slide-img {
        width: 800px;
        z-index: -10;
        position: relative;
        img {
            height: 100%;
            position: absolute;
            top: 0; left: 50%;
            transform: translateX(-50%);
            opacity: 1;
            transition: 0.5s;
            &.on { opacity: 1; }
        }
    }
    .menu {
        width: 800px; height: 100%;
        padding: 50px 0;
        box-sizing: border-box;
        li {
            a {
                font-size: 50px;
                font-weight: 500;
                color: #999;
                &:hover {color: #a39a5a; }
            }
            &::after {
                content: "";
                display: block;
                width: 500px; height: 1px;
                background: #999;
                margin-top: 30px;
            }
            &:last-child::after { display: none; }
            margin-bottom: 30px;
        }
    }
    .close {
        padding: 20px 20px;
        box-sizing: border-box;
        i { cursor: pointer; font-size: 50px;
        color: #4e492c; }
    }
} */
`

// TamraeveSwiperBox
export const TamraeveSwiperBox = styled.div`
`

// TamraeveContainer
export const TamraeveContainer = styled.section`
`

// TamraeveFooterBox
export const TamraeveFooterBox = styled.ul`
/* position:relative; margin-top:36px;
&:before { content:''; position:absolute; left:0; top:50%; display:block; width:100%; height:1px; background:#474747; }
li {
    position:relative; display:inline-block; padding:0 20px; background:#303030;
    &:before { content:''; position:absolute; left:0; top:50%; display:block; width:3px; height:3px; margin-top:-2px; background:#989898; }
    &:first-child:before {display:none;}
} */
`